apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-ui
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-uix
  template:
    metadata:
      labels:
        app: guestbook-uix
    spec:
      containers:
      - image: nginx
        name: guestbook-uix
        ports:
        - containerPort: 80
